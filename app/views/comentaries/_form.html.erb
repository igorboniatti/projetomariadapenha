<%= form_with(model: comentary, local: true) do |form| %>
  <% if comentary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comentary.errors.count, "error") %> prohibited this comentary from being saved:</h2>

      <ul>
      <% comentary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :publication_id %>
    <%= form.text_field :publication_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
