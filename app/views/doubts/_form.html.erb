<%= form_with(model: doubt, local: true) do |form| %>
  <% if doubt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doubt.errors.count, "error") %> prohibited this doubt from being saved:</h2>

      <ul>
      <% doubt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_area :answer %>
  </div>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
